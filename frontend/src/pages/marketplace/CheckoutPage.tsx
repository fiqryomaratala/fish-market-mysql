import { PagePlaceholder } from '@/components/common/PagePlaceholder'

function CheckoutPage() {
  return (
    <PagePlaceholder
      eyebrow="Marketplace"
      title="Checkout"
      description="Flow checkout untuk alamat, metode pembayaran, ringkasan pesanan, dan konfirmasi transaksi."
    />
  )
}

export default CheckoutPage
